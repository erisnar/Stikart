{
    "name": "stikart",
    "private": true,
    "description": "Dev dependencies only — the app itself has no build step and no runtime npm dependencies.",
    "scripts": {
        "test": "playwright test",
        "test:headed": "playwright test --headed",
        "test:ui": "playwright test --ui"
    },
    "devDependencies": {
        "@playwright/test": "^1.53.0"
    }
}
